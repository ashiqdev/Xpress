const User = require('./User');
const Service = require('./Service');
const Review = require('./Review');
const Order = require('./Order');

const models = { User, Service, Review, Order };

module.exports = models;
